<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit EJS</title>
    <script src="https://cdn.tailwindcss.com"></script>    
</head>
<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white p-10">
        <!-- logout button-->
        <div class=" w-full flex justify-end">
        <a class="bg-red-500 rounded-md px-3 py-2 text-sm  mb-5 inline-block" href="/profile">Profile</a> 
        </div>
        
     <h5 class="mb-5 font-bold">Edit Your Blog:</h5>
        <form method="post" action="/update/<%= post._id %>">
           <textarea placeholder="What's on you Mind ?" class="font-bold block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md" name="content"><%= post.content %></textarea>
           <button class="px-3 py-2 w-40 text-sm bg-yellow-500 font-bold text-black block rounded-md mt-2" type="submit">Update  Post</button>
        </form>
    </div>    
</body>
</html>